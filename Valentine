<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Valentine?</title>
  <style>
    body {
      margin: 0;
      font-family: system-ui, -apple-system, Segoe UI, Roboto, Arial, sans-serif;
      display: grid;
      place-items: center;
      min-height: 100vh;
      background: linear-gradient(135deg, #ffd6e7, #ffe9c6);
      color: #1a1a1a;
    }
    .card {
      width: min(560px, 92vw);
      background: rgba(255,255,255,0.85);
      border: 1px solid rgba(0,0,0,0.06);
      border-radius: 20px;
      padding: 28px;
      box-shadow: 0 20px 60px rgba(0,0,0,0.12);
      text-align: center;
      position: relative;
      overflow: hidden;
    }
    h1 {
      margin: 0 0 10px;
      font-size: 2rem;
      letter-spacing: -0.02em;
    }
    p {
      margin: 0 0 20px;
      font-size: 1.05rem;
      line-height: 1.45;
      opacity: 0.9;
    }
    .buttons {
      display: flex;
      justify-content: center;
      gap: 12px;
      flex-wrap: wrap;
      position: relative;
      padding: 10px 0 0;
    }
    button {
      border: 0;
      padding: 12px 18px;
      border-radius: 999px;
      font-size: 1rem;
      cursor: pointer;
      transition: transform 0.08s ease, opacity 0.2s ease;
      user-select: none;
    }
    button:active { transform: scale(0.98); }
    #yesBtn {
      background: #ff3b7f;
      color: white;
      box-shadow: 0 10px 25px rgba(255,59,127,0.25);
    }
    #noBtn {
      background: #ffffff;
      color: #1a1a1a;
      border: 1px solid rgba(0,0,0,0.12);
      position: relative;
    }
    .hidden { display: none; }

    /* cute little heart confetti */
    .heart {
      position: fixed;
      top: -10px;
      font-size: 18px;
      pointer-events: none;
      animation: fall 2.2s linear forwards;
    }
    @keyframes fall {
      to { transform: translateY(110vh) rotate(360deg); opacity: 0; }
    }
  </style>
</head>
<body>
  <div class="card" id="card">
    <div id="ask">
      <h1>Will you be my Valentine? 💘</h1>
      <p>I don’t care how many miles are between us. I choose you.</p>

      <div class="buttons" id="btnArea">
        <button id="yesBtn">Yes 💖</button>
        <button id="noBtn">No 🙃</button>
      </div>
    </div>

    <div id="love" class="hidden">
      <h1>YAYYYYY 💞</h1>
      <p id="loveText">
        I love you so much. You make my life feel lighter, funnier, and better in every way.
        I’m so grateful it’s you. Long distance can’t change that — you’re my favorite person.
        Happy Valentine’s Day, beautiful. 💗
      </p>
      <p style="margin-top:14px; font-size:0.95rem; opacity:0.75;">
        (Screenshot this and send it to me 😌)
      </p>
    </div>
  </div>

  <script>
    const yesBtn = document.getElementById("yesBtn");
    const noBtn = document.getElementById("noBtn");
    const ask = document.getElementById("ask");
    const love = document.getElementById("love");

    // Make "No" dodge the cursor/tap
    function moveNoButton() {
      const pad = 16;
      const area = document.getElementById("card").getBoundingClientRect();
      const btn = noBtn.getBoundingClientRect();

      const maxX = area.width - btn.width - pad * 2;
      const maxY = area.height - btn.height - pad * 2;

      const x = Math.random() * maxX + pad;
      const y = Math.random() * maxY + pad;

      noBtn.style.position = "absolute";
      noBtn.style.left = x + "px";
      noBtn.style.top = y + "px";
    }

    noBtn.addEventListener("mouseenter", moveNoButton);
    noBtn.addEventListener("touchstart", (e) => { e.preventDefault(); moveNoButton(); }, { passive: false });
    noBtn.addEventListener("click", moveNoButton);

    // Confetti hearts
    function heartsConfetti() {
      const hearts = ["💗","💖","💘","💞","💕"];
      for (let i = 0; i < 28; i++) {
        const el = document.createElement("div");
        el.className = "heart";
        el.textContent = hearts[Math.floor(Math.random() * hearts.length)];
        el.style.left = Math.random() * 100 + "vw";
        el.style.animationDuration = (1.6 + Math.random() * 1.4) + "s";
        el.style.fontSize = (14 + Math.random() * 18) + "px";
        document.body.appendChild(el);
        setTimeout(() => el.remove(), 2600);
      }
    }

    yesBtn.addEventListener("click", () => {
      ask.classList.add("hidden");
      love.classList.remove("hidden");
      heartsConfetti();
    });
  </script>
</body>
</html>
